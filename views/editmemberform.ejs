<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Member</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 20px;
      }

      h1 {
        color: #2575fc;
      }

      form {
        margin-top: 20px;
        display: flex;
        flex-direction: column;
        gap: 10px;
        max-width: 400px;
      }

      label {
        font-weight: bold;
      }

      input,
      select,
      button {
        padding: 10px;
        font-size: 16px;
        border: 1px solid #ddd;
        border-radius: 5px;
      }

      button {
        background-color: #2575fc;
        color: white;
        cursor: pointer;
      }

      button:hover {
        background-color: #1a5fc1;
      }
    </style>
  </head>
  <body>
    <h1>Edit Member</h1>
    <form action="/api/search/editmember/<%= member._id %>" method="post" enctype="multipart/form-data">
      <label for="memberGrade">Member Grade</label>
      <select name="memberGrade" id="memberGrade" required>
        <option value="starter member" <%= member.memberGrade === "starter" ? "selected" : "" %>>Starter</option>
        <option value="bronze member" <%= member.memberGrade === "bronze" ? "selected" : "" %>>Bronze</option>
        <option value="silver member" <%= member.memberGrade === "silver" ? "selected" : "" %>>Silver</option>
        <option value="gold member" <%= member.memberGrade === "gold" ? "selected" : "" %>>Gold</option>
        <option value="platinum member" <%= member.memberGrade === "platinum" ? "selected" : "" %>>Platinum</option>
        <option value="diamond member" <%= member.memberGrade === "diamond" ? "selected" : "" %>>Diamond</option>
      </select>

      <label for="icon_file">Member Icon</label>
      <input type="file" name="icon_file" id="icon_file" accept="image/*" />

      <label for="memberPoints">Member Points</label>
      <input type="number" name="memberPoints" id="memberPoints" value="<%= member.memberPoints %>" required />

      <button type="submit">Save Changes</button>
    </form>
  </body>
</html>
